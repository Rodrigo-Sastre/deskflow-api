namespace DeskFlow.API.Models
{
    public enum Prioridade
    {
        Baixa = 1,
        Media = 2,
        Alta = 3
    }

    public enum Status
    {
        Aberto = 1,
        EmAndamento = 2,
        Fechado = 3
    }
}